// let class = "Hello World"
// // classs is a reserved keyword in JS, so it can't be used as an identifier. This will result in a syntax error.

// let return = "Hello World"
// // return is a reserved keyword in JS, so it can't be used as an identifier. This will result in a syntax error.

// Similarily if, for, do , while

let myVariable = 10;
// This is a valid identifier

// These look like keyword but are not reserved
let undefined = 5; // Work but never do this
let infinity = 10;

