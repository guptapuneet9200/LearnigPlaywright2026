//  Step 1 Defination of functions
function getUser() {

    console.log(status_code)
    var status_code = "Active"
    console.log(status_code)

}
// Step 2 Calling of the function
getUser();

// Note : var is function scoped, so status is hoisted to
// the top of getUser(), Not the global scope.
